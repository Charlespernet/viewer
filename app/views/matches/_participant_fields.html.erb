<div class="nested-fields">
  <%= f.association :player, collection: @players %>
  <%= f.association :race, collection: @races %>
  <%= f.input :winner, as: :radio_buttons %>
  <%= link_to_remove_association "remove participant", f %>
</div>
